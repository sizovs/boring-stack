version: 3

env:
  DB_LOCATION: ":memory:"
  NODE_ENV: development
  COOKIE_SECRET: unsecure

tasks:
  terraform:
    cmds:
      - terraform -chdir=terraform {{.CLI_ARGS}}
  test:
    cmds:
      - npm test
      - npx playwright test
  dev:
    env:
      DB_LOCATION: ./modules/database/db.sqlite3
    cmds:
      - npm install
      - npm run dev
  db:pull:
    cmds:
      - rm -rf ./tmp
      - litestream restore -config ./litestream.yml $DB_LOCATION
  deploy:
    silent: true
    vars:
      ip: xxx.xxx.xxx.xxx
    cmds:
      - echo Will deploy to {{.ip}}
      - rsync -a ./ devops@{{.ip}}:/home/devops/latest
      - ssh devops@{{.ip}} "DOMAIN=https://dev.club R2_BACKUP_ENDPOINT=$R2_BACKUP_ENDPOINT R2_BACKUP_BUCKET=$R2_BACKUP_BUCKET R2_BACKUP_KEY=$R2_BACKUP_KEY R2_BACKUP_SECRET=$R2_BACKUP_SECRET bash -s" < ./deploy.sh
